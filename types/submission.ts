export interface Submission {
  _id: string;
  // Añadir aquí otras propiedades relevantes de Submission si las necesitas
  // Por ejemplo:
  // data: any;
  // formId: string;
  // submittedBy: string;
  // createdAt: string;
}